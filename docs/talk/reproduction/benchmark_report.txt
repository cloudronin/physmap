PhysMAP Benchmark v0.4.1 (cross-domain, real-API, registry-driven)

Bank v0.4.1 (data/benchmarks/v0_4_1/), current. It corrects the NACA source data: the original
bank's NACA row came from an automated read of the figure later found invalid. Kept unchanged
for audit, not as evidence: v0.4 at data/benchmarks/v0_4/. See data/naca/CORRECTION_v0_4_1.md.

All 7 vehicles are reported. 7 of 7 ship their source data -- all of them.

NOTHING WAS RECOMPUTED IN THIS RUN. Every number below is read from the banked matrix. `physmap benchmark run` recomputes them from this checkout and diffs the result against that bank.

vehicle                            domain         failure var            observability  outcome           source
----------------------------------------------------------------------------------------------------------------
naca_tn1451                        thermal-fluids x_over_D               unobservable   PHYSMAP_WINS      ships, licensed
velazquez_sco2                     thermal-fluids ratio_mu_w_b           partial        PARTIAL           ships, licensed
forrest                            thermal-fluids Re                     observable     DO_NO_HARM        ships, AGAINST publisher terms
casper_hypersonic_transition       aerospace      freestream_noise_pct   unobservable   PHYSMAP_WINS      ships, AGAINST publisher terms
marineau_hypersonic_transition     aerospace      st_xsw_ratio           observable     BASELINE_VISIBLE  ships, NOT licensed (facts basis)
dirker_water                       thermal-fluids Ri                     partial        PARTIAL           ships, AGAINST publisher terms
jin_sco2_buoyancy                  thermal-fluids Bu                     unobservable   PHYSMAP_WINS      ships, AGAINST publisher terms

observability guards: all passed

What the closure-validity check adds to the input-based detectors
  at the reference operating percentile 99. Counts are rows of each dataset, not
  independent cases, and the datasets are not pooled.

  vehicle                            failure variable  wrong: caught only by PhysMAP   right: flagged anyway
  naca_tn1451                        unobservable      9 of 9                          19
  velazquez_sco2                     partial           18 of 67                        0
  forrest                            observable        not tested -- 1 training row(s), no detector fit
  casper_hypersonic_transition       unobservable      4 of 8                          0
  marineau_hypersonic_transition     observable        0 of 6                          0
  dirker_water                       partial           2 of 11                         16
  jin_sco2_buoyancy                  unobservable      15 of 26                        0

  Whether a vehicle's count shows a blind spot that deployment created depends
  on its home baseline, reported next.

Home baseline -- is each surrogate accurate where it is meant to work?
  A wrong deployment prediction that only PhysMAP flags shows a failure that
  deployment created only if the surrogate was accurate at home. Wrong means the
  benchmark's own threshold. Every row and every count above stands. No gate
  decides the reading: the counts and rates are the evidence, and each vehicle's
  interpretation is written prose.

  casper_hypersonic_transition
    surrogate: a Gaussian process on (Mach, unit Reynolds, axial x); home: the noisy-tunnel rows, which the Gaussian process is fitted to
    in-sample fit error: 2 of 159 home rows wrong (1%)
    held-out home error (leave-one-out refit): 6 of 159 wrong (4%)
    deployment: 8 of 8 wrong (100%), threshold 30.6%
    interpretation: Held out, the surrogate is wrong on 6 of 159 home rows (4%); deployed on the quiet tunnel, on 8 of 8 (100%). The failure appears on deployment, and its cause, freestream noise, is not a surrogate input.
  dirker_water
    surrogate: a straight-line fit of Nu against log10(Re); home: the low-Richardson rows, which the line is fitted to
    in-sample fit error: 0 of 31 home rows wrong (0%)
    held-out home error (leave-one-out refit): 0 of 31 wrong (0%)
    deployment: 11 of 60 wrong (18%), threshold 12.9%
    interpretation: Held out, the straight-line surrogate is wrong on 0 of 31 low-Richardson home rows; deployed, on 11 of 60 (18%). The failures appear as buoyancy grows, a cause the surrogate's inputs see only partly.
  forrest
    surrogate: the modified Sparrow-Cur correlation (2014); home: the rows the vehicle's split assigns to training
    held-out home error (published correlation, not fitted to these rows): 0 of 1 wrong (0%)
    deployment: 4 of 4 wrong (100%), threshold 12.7%
    interpretation: One home row, so there is no home baseline; its values are visual triage estimates, not benchmark-grade.
  jin_sco2_buoyancy
    surrogate: the constant-property Dittus-Boelter correlation; home: the rows the vehicle's split assigns to training
    held-out home error (published correlation, not fitted to these rows): 17 of 17 wrong (100%); 11 of 17 inside the correlation's validated range
    deployment: 26 of 27 wrong (96%), threshold 12.7%
    interpretation: The constant-property correlation is wrong on 17 of 17 home rows (100%) and 26 of 27 deployed rows (96%); 11 of 17 home rows lie inside its validated range. It is wrong almost everywhere in this near-critical CO2 data, so the counts cannot show that deployment created the failure.
  marineau_hypersonic_transition
    surrogate: a Gaussian process on (unit Reynolds, nose radius); home: the benign-regime rows, which the Gaussian process is fitted to
    in-sample fit error: 0 of 9 home rows wrong (0%)
    held-out home error (leave-one-out refit): 5 of 9 wrong (56%)
    deployment: 6 of 6 wrong (100%), threshold 12.7%
    interpretation: In-sample the surrogate looks perfect (0 of 9 wrong); held out, it is wrong on 5 of 9 home rows (56%); deployed, on 6 of 6. Nine home rows cannot separate the two. Its role is the control where the cause is a surrogate input: the input-based detectors see it, and PhysMAP adds nothing.
  naca_tn1451
    surrogate: the Gnielinski correlation (1976); home: the fully developed rows (x/D at or above the entrance cut)
    held-out home error (published correlation, not fitted to these rows): 0 of 40 wrong (0%); 40 of 40 inside the correlation's validated range
    deployment: 9 of 45 wrong (20%), threshold 17.5%
    interpretation: Gnielinski is wrong on 0 of 40 fully developed home rows and on 9 of 45 entrance rows (20%), all near the inlet. The failures appear in the entrance region, which the correlation does not cover.
  velazquez_sco2
    surrogate: the constant-property Gnielinski correlation; home: the rows the vehicle's split assigns to training
    held-out home error (published correlation, not fitted to these rows): 386 of 393 wrong (98%); 197 of 393 inside the correlation's validated range
    deployment: 67 of 67 wrong (100%), threshold 12.7%
    interpretation: The constant-property correlation is wrong on 386 of 393 home rows (98%) and 67 of 67 deployed rows (100%); only 197 of 393 home rows lie inside its validated range. It is wrong almost everywhere, so the counts cannot show that deployment created the failure.

  Exploratory only: a one-sided Fisher exact test of deployment error against held-out home error, chosen after the counts were seen. It decides nothing.
    casper_hypersonic_transition p = 2.4e-10; dirker_water p = 0.0072; forrest p = 0.2; jin_sco2_buoyancy p = 1; marineau_hypersonic_transition p = 0.092; naca_tn1451 p = 0.0022; velazquez_sco2 p = 0.33

Does the kind of model matter? The architecture axis
  The detectors never look inside the model, so they flag the same rows for any
  surrogate. Three model types are trained on each vehicle below; each must first
  pass the same accuracy gate. Count: wrong predictions that only PhysMAP flags, at
  percentile 99, per model type.

  vehicle                         gp            deeponet      gbt           
  casper_hypersonic_transition    4 of 8        4 of 8        4 of 8        
  jin_sco2_buoyancy               fails gate    fails gate    fails gate    
  naca_tn1451                     11 of 11      11 of 11      11 of 11      
  velazquez_sco2                  fails gate    fails gate    fails gate    

  Two or more model types pass the gate on 2 of 4 vehicles: casper_hypersonic_transition, naca_tn1451.
  On naca_tn1451 the gate says little: its 40 home rows hold 5 distinct values, so leave-one-out keeps copies of each held-out row.
  `physmap benchmark architectures` recomputes this; it needs PyTorch.

Redistribution basis -- read this before reusing any of this data

  2 of the 7 shipped datasets carry affirmative permission: public domain or an open licence.
  5 ship WITHOUT a licence: forrest, casper_hypersonic_transition, marineau_hypersonic_transition, dirker_water, jin_sco2_buoyancy.
  Shipping is not licensing. They rest on the position that measured values
  are facts.
  4 of them are published AGAINST an express publisher
  term rather than under one: forrest, casper_hypersonic_transition, dirker_water, jin_sco2_buoyancy.
  Only the numbers are redistributed; no paper, figure or PDF. They are
  removed on objection -- see NOTICE.

Data quality -- separate from redistribution, and not implied by it

  Not benchmark-grade by their own account: forrest.
  A dataset can be perfectly legal to publish and still be unfit to
  benchmark on. Their cells are reported so the weakness is
  inspectable, not so it can be cited.

Provenance and redistribution basis

  naca_tn1451
    source      NACA TN-1451 (1947), Fig 10, bellmouth entrance
    values      Two-reader visual digitisation
    ships       yes (clear)
    licensed    yes
    basis       US Government work, public domain.

  velazquez_sco2
    source      Velazquez et al. (2026), Appl. Therm. Eng. 285:129206
    values      Verbatim transcription of supplementary Appendix D, Tables D1-D28
    ships       yes (clear)
    licensed    yes
    basis       CC BY 4.0, publisher-deposited. Elsevier states the article licence extends to supplementary files.

  forrest
    source      Forrest et al., J. Heat Transfer 138(2):021704
    values      Visual estimates from Fig 5 of the version of record
    ships       yes (against_publisher_terms)
    licensed    NO
    QUALITY     TRIAGE_ONLY -- The file's own header: 'VISUAL ESTIMATES ... NOT digitized by WebPlotDigitizer. Use ONLY as a resolvability triage check.' Its benchmark cell is also degenerate -- 1 training row, no detector fit -- so its DO_NO_HARM outcome is short-circuited rather than earned. Published so both weaknesses are visible; re-digitise Fig 5 properly before treating this cell as evidence.
    basis       ASME holds copyright and requires written permission to reproduce. The CC-BY route does not apply: ASME grants it for accepted manuscripts in INSTITUTIONAL repositories, the OSTI deposit is a FUNDER repository, and the deposited PDF carries no Creative Commons marking at all. Shipped anyway, as an accepted risk. Numbers only.

  casper_hypersonic_transition
    source      Casper MS thesis (DTIC ADA504177) and AIAA 2009-4054
    values      Figure digitisation, 600-DPI segmentation, two readers
    ships       yes (against_publisher_terms)
    licensed    NO
    basis       The rows carrying the result come from the AIAA paper, and AIAA prohibits using their content to develop machine-learning models without written permission -- which is what a public ML benchmark is. Shipped anyway, as an accepted risk. Weaker than Marineau, not equal to it: the OSTI copy carries no copyright notice AND no public-release marking, just silence. Numbers only.

  marineau_hypersonic_transition
    source      Marineau et al., AIAA 2014-3108 / SAND2014-4326C
    values      Transcribed from Table 3
    ships       yes (no_licence_facts_basis)
    licensed    NO
    basis       No reuse licence exists, and no prohibition either. The OSTI copy carries no copyright notice and is marked public-release-unlimited by the controlling office -- a security determination, not a licence, and OSTI expressly disclaims granting one. Ships on the position that measured values transcribed from a published table are facts. Government-funded, government-hosted, transcribed rather than digitised: the cleanest of the unlicensed three.

  dirker_water
    source      Dirker, Meyer & Reid (2018), Exp. Therm. Fluid Sci. 98
    values      Figure digitisation from Figs 17-20
    ships       yes (against_publisher_terms)
    licensed    NO
    basis       Elsevier's TDM licence forbids substantially or systematically redistributing the dataset, and the one permissions exemption is drafted to exclude data that was previously in figure format -- which this was. Shipped anyway, as an accepted risk. Values are numbers only; no figure, text or PDF is redistributed. Weakest of the three: read off plots rather than transcribed.

  jin_sco2_buoyancy
    source      Jin et al. (2023), Ann. Nucl. Energy 188:109825
    values      Figure digitisation, two readers; Bu and Bo* recomputed
    ships       yes (against_publisher_terms)
    licensed    NO
    basis       Same Elsevier terms as Dirker, with no open copy anywhere, and the authors were asked for raw data and declined. Shipped anyway, as an accepted risk. Bu and Bo* are recomputed from the paper's own equations, so part of this file is derived work rather than extraction.

What the rerunnable subset covers

  domains       full: aerospace, thermal-fluids
                public: aerospace, thermal-fluids
  outcomes      full: BASELINE_VISIBLE, DO_NO_HARM, PARTIAL, PHYSMAP_WINS
                public: BASELINE_VISIBLE, DO_NO_HARM, PARTIAL, PHYSMAP_WINS
  observability full: observable, partial, unobservable
                public: observable, partial, unobservable

  Outcome classes whose ONLY vehicle is not benchmark-grade: DO_NO_HARM.
    DO_NO_HARM rests on forrest: n_train=1, n_test=4 -- observable degenerate pole; only 1 train rows (thin) — baseline-sufficient by construction, no detector fit (matches degenerate_short_circuit)

Full determinations: data/REDISTRIBUTION.md
